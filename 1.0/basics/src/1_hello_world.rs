fn main() {
    println!("Hello, world!");
}

// main is a special function that denotes the start of a rust program
// println! is a macro rather than a function
// the string is statically allocated
// ; is the end of an expression

// rust is a compiled language
// rustc hello_world.rs gives us a binary executable
// ./hello_world executes it
